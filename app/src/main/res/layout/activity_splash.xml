<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_vice"
    tools:context=".activity.SplashActivity">

    <ImageView
        android:layout_width="170dp"
        android:layout_height="170dp"
        android:layout_above="@id/splash_text"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/app_name"
        android:outlineAmbientShadowColor="@color/grey_60"
        android:padding="10dp"
        android:src="@drawable/sinergi"
        tools:targetApi="p" />

    <dev.affan.sinergi.widget.GradientTextView
        android:id="@+id/splash_text"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/app_name"
        android:textSize="36sp"
        app:color_end="@color/mild_end"
        app:color_start="@color/mild_start"
        app:with_shadow="true" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <ProgressBar
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            android:padding="20dp" />
        <TextView
            android:layout_width="match_parent"
            android:textAlignment="center"
            android:textSize="10sp"
            android:padding="10dp"
            android:text="@string/notice"
            android:layout_height="wrap_content" />
    </LinearLayout>

</RelativeLayout>
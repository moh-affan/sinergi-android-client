<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.Fullscreen">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AlerterButtonPrimary" parent="AlertButton">
        <item name="android:colorAccent" tools:targetApi="lollipop">@color/primary</item>
    </style>

    <style name="AlerterButtonDanger" parent="AlertButton">
        <item name="android:colorAccent" tools:targetApi="lollipop">@color/danger</item>
    </style>

    <style name="AlerterButtonSuccess" parent="AlertButton">
        <item name="android:colorAccent" tools:targetApi="lollipop">@color/success</item>
    </style>

    <style name="AlerterButtonWarning" parent="AlertButton">
        <item name="android:colorAccent" tools:targetApi="lollipop">@color/warning</item>
    </style>

    <style name="AlerterButtonDefault" parent="AlertButton">
        <item name="android:colorAccent" tools:targetApi="lollipop">@color/grey_80</item>
    </style>

    <style name="ToolbarTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textAlignment">center</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

</resources>
